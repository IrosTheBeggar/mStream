// Function that scans user folder and returns array of all new songs to process
  // Make this a generator so it can work in pieces

// Function that can spawn new process for scannign user folders
  // Queue scans so we don't start too many at a time
